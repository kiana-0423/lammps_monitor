# Runtime data root

This tree is the only repository-local location for generated operational data.
Its directory skeleton is tracked; all actual contents are ignored by Git.

Do not place runtime artifacts in source, configuration, documentation, or
container build directories.
